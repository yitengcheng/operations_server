const handleOption = require('./handleOption');
const optionTable = require('./optionTable');
const delOption = require('./delOption');
const options = require('./options');

module.exports = {
  handleOption,
  optionTable,
  delOption,
  options,
};
