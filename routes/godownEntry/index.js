const handleGood = require('./handleGodownEntry');
const goodTable = require('./godownEntryTable');
const invalidGodownEntry = require('./invalidGodownEntry');
const godownEntry = require('./godownEntry');

module.exports = {
  handleGood,
  goodTable,
  invalidGodownEntry,
  godownEntry,
};
